import { SearchContainer, CarouselContainer } from "../components";

const Home = () => {

  return (
    <section className="home">
      <SearchContainer />
      <CarouselContainer />
    </section>
  );
}

export default Home;